{
  "name": "freelog-docs",
  "version": "0.0.0",
  "scripts": {
    "start": "set NODE_OPTIONS=--openssl-legacy-provider &  vuepress dev",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider &  vuepress build"
  },
  "devDependencies": {
    "vuepress": "^1.9.9"
  },
  "dependencies": {
    "@vuepress/core": "^1.9.9"
  }
}
